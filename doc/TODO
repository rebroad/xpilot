****XPilotControl****
1. Flesh out XPWhereWindow.
	A. Joining specific teams
	B. List LAN servers separate (Different class of ServerList necessary to search
		only for LAN servers.)

2. Update Server tab to add proper server controls.
	Required: List widget for players (Possibly common control for client scores)

3. Flesh out ClientConfig for client settings.

4. Support connecting of Daemonized server.

5. Determine support for recording. Needs to be sync'd with client.
(Recording should still be possible, but the format may COMPLETELY change.
maybe server recording would be better used than client side.)

6. Proper option filtering for ServerPrefs Dialog

7. ServerList needs to contact both metaservers and make pretty ping times
	like welcome.c

8. Rename to XPilot.  client becomes XPilotClient.


****Server****
1. Daemonize server
2. Switch robots to using same format as normal ships, with added robot options
3. Handle custom bitmaps for ships, specifiable in the shipfile
	(This will require changes to editor and client as well. Shipeditor will need
	to be able to paint pixmaps over the ship.)
4. Ability to specify certain pixmaps for certain blocks would be nice.
	(Could be worked into new XML map format, if that happens.)
5. "talk" bug that sometimes prevents clients from talking needs fixed.
6. Some form of scripting support would be nice.

****Client****
1. FLTK client

****ScoreServer****
1. Need team scoring engine.
2. Finish HTML admin screen.

****System****
1. Move lib/kde to install/kde.

2. Move src/common/NT/bindist to install/win32.
